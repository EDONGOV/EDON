# Archived pages (not in nav or routes)

These pages were removed from the live site. Routes redirect to `/`.
Restore by moving back to `src/pages/` and re-adding routes + nav links in App, Footer, FooterDark.

- Standards.tsx
- Platforms.tsx
- About.tsx
- News.tsx
